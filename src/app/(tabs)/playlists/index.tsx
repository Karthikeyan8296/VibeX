import React from 'react'
import { Text, View } from 'react-native'

const PlaylistScreen = () => {
	return (
		<View className="flex-1 bg-background">
			<Text className="text-text text-base">Playlist Screen</Text>
		</View>
	)
}

export default PlaylistScreen
